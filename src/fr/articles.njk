---
permalink: fr/articles.json
layout: blank
---

{
	"articles": [{% for item in collections.articlesFrancaisTries %}
			{
				"url": "{{ item.url | url }}",
				"daysLeft": {{ item.data.daysLeft }},
				"index": {{ loop.index0 }}
			}
			{% if not loop.last %},{% endif %}{% endfor %}]
}
